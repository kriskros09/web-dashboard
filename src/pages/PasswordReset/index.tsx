import React, { ReactElement } from 'react'

import PasswordResetContainer from '../../containers/PasswordReset'

const PasswordResetPage: React.FC = (): ReactElement => <PasswordResetContainer />

export default PasswordResetPage
